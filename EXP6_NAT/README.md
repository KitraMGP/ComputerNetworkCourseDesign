# 实验六：NAT 网络地址转换原理演示

本程序依赖 Qt6 构建，用户可以通过交互式操作来学习 NAT 网络地址转换的原理。

## 运行

```bash
xmake run
```

## 使用说明

内网主机首先应该向公网主机发送数据（发送请求），路由器会为其分配一个专用端口用来与公网主机通信。建立 NAT 记录后，外网主机才能和对应的内网主机通信。

所有的网络地址都是`[IP地址:端口]`的形式。内网地址的示例：`192.168.1.10:10000`；外网地址的示例：`8.8.8.8:80`。外网向内网发送数据的目的地址只能是路由器地址+路由器分配的端口。
